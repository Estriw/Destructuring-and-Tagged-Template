const textMultiLine = `testing 
multiline`

const embededd = 200
console.log(`nilai yg di print ${embededd}`);

const htmlFragments = `<div>
                         <span>title</span>
                        </div>`

const interpolation = 3
console.log(`angka ${interpolation % 2 == 0 ? 'Genap' : 'Ganjil'}`); 